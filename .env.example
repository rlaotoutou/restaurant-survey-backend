# Allowed CORS origin(s), comma-separated. Example:
# CORS_ORIGIN=https://your-frontend.example.com,https://another.site
CORS_ORIGIN=*

# Optional: protect admin routes (/api/surveys, /api/export)
ADMIN_KEY=change-me

# Where to store SQLite DB file. For Railway volume, set to /data/surveys.db
DB_FILE=./data/surveys.db
